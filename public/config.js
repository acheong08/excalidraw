// Runtime configuration placeholder
// In Docker deployments, this file is replaced by docker-entrypoint.sh
// with actual environment variable values at container startup.
// For non-Docker deployments, the app falls back to build-time values.
window.__EXCALIDRAW_CONFIG__ = {};
